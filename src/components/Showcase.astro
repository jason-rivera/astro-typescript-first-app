---
const { heading, description } = Astro.props as Props;

interface Props {
  heading: string;
  description: string;
}
---

<section>
  <h1>{heading}</h1>
  <p>{description}</p>
</section>

<style>
  section {
    display: flex;
    align-items: center;
    flex-direction: column;
    background-image: linear-gradient(#3a371d, rgb(14, 48, 22));
    border: none;
    margin: 0px;
    padding: 40px;
    height: 200px;
    color: white;
  }

  h1 {
    font-size: 3rem;
  }
</style>
